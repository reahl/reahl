name: Release

on:
  release:
    types:
      [created]

  workflow_dispatch:

jobs:
  call_build:
    uses: ./.github/workflows/build.yml

  deploy:
    needs: call_build
    environment: release-builds
    runs-on:  ubuntu-20.04
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
        with:
          python-version: '3.8'

      - name: Install dependencies
        run: python -m pip install --upgrade pip

      - name: Download wheels
        uses: actions/download-artifact@v2
        with:
          name: wheels
          path: ~/.reahlworkspace/dist-egg

      - name: Setup Test env
        run: |
          sudo ./scripts/installDebs.sh
          sudo ./scripts/installDevEnvDebs.sh
          ./scripts/setupDevEnv.sh

      - name: Install reahl
        run: pip install --pre reahl-dev reahl-commands #uses wheels as specified in $HOME/.pip/pip.conf

      - name: Select all reahl projects
        run: . $HOME/.profile && reahl refresh && reahl select -a

      - name: Sign all wheels
        run: |
          . $HOME/.profile && reahl sign -sdX && reahl list -s

      - name: Publish
        run: |
          ls -la /home/runner/.reahlworkspace/dist-egg/
          . $HOME/.profile && ./travis/configurePypi.sh && reahl upload -rusdX

      - name: Cleanup
        if: ${{ always() }}
        run: |
          find $HOME/.gnupg/ -type f -exec shred -fu {} \;

Zjk1MTQ2YTFiZGU2N2Y5YWFiZThiNDQwY2QwMDQ2NjU5OTkzM2E1ZQ==:MTY0NTY4Njg3NS45MDE3NTk=:6fed67f7a012f046b31cb4cbf36ed681fae73664
Zjk1MTQ2YTFiZGU2N2Y5YWFiZThiNDQwY2QwMDQ2NjU5OTkzM2E1ZQ==:MTY0NTY4Njg3Ni4wNTY3MjE=:8b535c1f0706e3422debf4233ebc13c34da9d4db
Zjk1MTQ2YTFiZGU2N2Y5YWFiZThiNDQwY2QwMDQ2NjU5OTkzM2E1ZQ==:MTY0NTY4Njg3Ny4zNDk5Mjc=:d72955f5966b3430b7d566ab3b42a9613c366737