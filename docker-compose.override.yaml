version: "3.9"
services:
  reahl:
    volumes:
      - type: bind
        source: ~/.bash_history_docker
        target: /home/developer/.bash_history
      - $REAHL_EXAMPLE_CHECKOUT_DIR:/tmp/examples
    environment:
      BOOTSTRAP_REAHL_SOURCE: /home/developer/reahl

  postgres:
    container_name: postgres
    image: postgres:12.3
    restart: always
    environment:
        POSTGRES_PASSWORD: reahl
        POSTGRES_USER: developer
    
#  mysql:
#    container_name: mysql
#    image: mysql:5.7
#    command: --default-authentication-plugin=mysql_native_password
#    restart: always
#    environment:
#      MYSQL_ROOT_PASSWORD: reahl

  #docker-compose build travis-cli
  #docker-compose run --rm travis-cli status
  #docker-compose run --rm travis-cli encrypt --skip-completion-check SOMEVAR="secretvalue"
  #alias travis-cli='docker run --rm -v $(pwd):/travis -w /travis travis-cli'
  travis-cli:
    container_name: travis-cli
    image: travis-cli:latest
    restart: never
    profiles:
      - dontstart
    build:
      context: .
      dockerfile: Dockerfile.travis-cli
    
