[build-system]
requires = ["setuptools >= 51.0.0, <= 62.1.0", "wheel","setuptools-git >= 1.1", "pytest-runner", "toml", "reahl-component-metadata", "pip >= 21.1"]
build-backend = "setuptools.build_meta"

